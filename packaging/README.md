# packaging/

Per-channel manifest scaffolds that implement
[`Docs/install-distribution-strategy.md`](../Docs/install-distribution-strategy.md)
(v1 confirmed plan).

## Layout

```
packaging/
├── winget/                 winget manifest scaffold (psmon.CodeScan, portable)
│   ├── README.md
│   └── manifests/p/psmon/CodeScan/0.0.0/
│       ├── psmon.CodeScan.installer.yaml
│       ├── psmon.CodeScan.locale.en-US.yaml
│       └── psmon.CodeScan.yaml
├── homebrew/               Tap formula template (psmon/homebrew-codescan)
│   ├── README.md
│   ├── codescan.rb
│   └── update-formula.sh
└── npm/codescan-cli/       Cross-platform npm wrapper (postinstall download)
    ├── README.md
    ├── package.json
    ├── bin/codescan.js
    └── scripts/{install,uninstall}.js
```

## Source of truth

All channels point at the same GitHub Release assets generated by
`.github/workflows/release.yml`:

```
codescan-win-x64.zip
codescan-linux-x64.tar.gz
codescan-linux-arm64.tar.gz
codescan-osx-arm64.tar.gz
checksums.txt
sbom.cdx.json
```

> v1 does not ship `codescan-osx-x64` (Intel Mac). GitHub Actions `macos-13` runner availability was too constrained for a reliable release pipeline. Intel Mac support is a v2 item.

Per-asset SHA256s live in `checksums.txt`. Each channel's "update" step pulls
SHA256s from there and rewrites the corresponding manifest template.

## v1 release procedure

1. **Cut a release**
   - Tag: `git tag v<x.y.z>; git push --tags`
   - Or run `release.yml` manually via `workflow_dispatch`.
   - CI builds the five matrix targets, generates SBOM, aggregates
     `checksums.txt`, and publishes a GitHub Release.

2. **Verify the release** — download one asset, run it, confirm
   `codescan --version` matches.

3. **Update channels (manual in v1)**
   - **winget**: see `packaging/winget/README.md`
   - **Homebrew**: see `packaging/homebrew/README.md`
   - **npm**:
     ```bash
     cd packaging/npm/codescan-cli
     npm version --no-git-tag-version <x.y.z>
     npm publish --access public
     ```

## v1.x automation

`.github/workflows/release.yml` includes three `if: false`-gated publisher jobs
(`publish-winget`, `publish-homebrew`, `publish-npm`). Enable them one channel
at a time after the manual flow has succeeded at least once and credentials
have been added to repo secrets:

| Job | Required secret |
|-----|-----------------|
| publish-winget | `WINGET_TOKEN` (PAT with PR rights on the `microsoft/winget-pkgs` fork) |
| publish-homebrew | `HOMEBREW_TAP_TOKEN` (PAT with push rights on `psmon/homebrew-codescan`) |
| publish-npm | `NPM_TOKEN` (npm automation token) |

## User-facing install paths

Documented in
[`Docs/install-distribution-strategy.md`](../Docs/install-distribution-strategy.md)
— summary:

| OS | Command |
|----|---------|
| Windows | `winget install psmon.CodeScan` |
| Linux   | `sudo npm install -g @webnori/codescan-cli` |
| macOS   | `brew install psmon/codescan/codescan` |

> The npm package is published under the scoped name `@webnori/codescan-cli`. The bare `codescan-cli` name on npm belongs to an unrelated third party (squat, broken on launch) — do not use it.

> Linux `sudo` is needed because system-Node's global npm prefix (`/usr/local/lib/node_modules/`) is root-owned. nvm/fnm-managed Node or `Script/install.sh` are sudo-free alternatives.

Direct script installers (no package manager) are at:

- `Script/install-win.ps1` (Windows)
- `Script/install.sh` (Linux/macOS)
